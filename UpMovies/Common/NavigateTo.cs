using System;
namespace UpMovies.Common
{
    // In many App this class would be called Routes, But I prefer calling it NavigateTo,
    // because it's easier to understend where we will call it
    public class NavigateTo
    {
        public const string Root = "NavigationPage/HomePage";
        public const string MovieDetail = "MovieDetailPage";
    }
}
